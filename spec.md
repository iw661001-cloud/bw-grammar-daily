# bw-grammar-daily 擴充規格（單字題庫＋知識庫回饋同步＋間隔重複複習）

> 本文件為這次擴充的設計決策唯一真實來源，程式碼須與本文件一致。
> 通用規範見 `D:\Users\bw5418\AI_SPEC.md`，網站設計規範見 `D:\Users\bw5418\WEB_DESIGN_SPEC.md`。
> 討論脈絡：2026-08-04 Claude Code 對話（先修復兩個既有 bug，後續延伸到題庫擴充規劃）。

---

## 0. 背景

`bw-grammar-daily` 目前有 5 個題庫（國中/高中/多益文法各10題、國中/高中中翻英各10題），這次擴充新增單字練習、把作答內容回饋進 Obsidian 知識庫、並把複習機制從「答錯才複習」升級成間隔重複排程。

**架構總決議：全部併入現有網站，不獨立建站**（沿用同一個 GitHub repo `iw661001-cloud/bw-grammar-daily`、同一個 Firebase 專案 `self_grammar` 集合）。理由：間隔重複複習佇列需要「今天到期的全部題目一起顯示」，拆成多個網站會讓複習佇列被拆散，違背做這個功能的目的。

---

## 1. 多益題庫調整

- 「多益/職場文法」更名為 **「多益 Part 5」**，題型從純文法改成**文法+字彙混合出題**（不再分開文法題/字彙題），因為真實 TOEIC Part 5 本來就是混合單句填空，考生無法預先知道這題考文法還是字彙。
- 現有 `toeic-grammar.json` 檔名與 track id（`toeic-grammar`）維持不變，只調整 `tracks.js` 裡的 `label` 顯示文字；之後新增的題目可以是字彙題也可以是文法題。
- **多益 Part 6（短文填空）、Part 7（閱讀測驗）**：本次只在首頁加「即將推出」佔位卡片，**不做實際功能**。未來若要做，會是同一個網站內的新頁面（例如 `toeic-reading.html`，文章＋多題版面），不是外部網站，理由同上（維持複習佇列統一）。

---

## 2. 新增單字題庫

新增 4 個 track（與現有文法/中翻英 track 同一套 `TRACKS` 陣列、同一套 `items`/`days` Firestore 結構），各起始 10 題：

| track id | 顯示名稱 | 字彙範圍依據 |
|---|---|---|
| `junior-vocab` | 國中單字 | 十二年國教課綱《國民中小學暨普通型高級中等學校語文領域-英語文》公告之基本1200字＋常用2000字參考字表 [L1，教育部課綱公告] |
| `senior-vocab` | 高中單字 | 大考中心《高中英文參考詞彙表》（111學年度起適用，官方文件本身約5,280詞，坊間常與全民英檢字表疊加後俗稱「高中7000字」，分基礎2200/核心2201-4400/進階4401-7000三級）[L2，2026-08-05查證修正原先「約6000字」的說法] |
| `toeic-vocab` | 多益單字 | 無官方字表，由 Claude 依常見職場/商用情境自行判斷選字、自行撰寫例句與解析；不整批照抄任何商業書籍或網站的選字清單或例句。與坊間資源（含使用者提供的我識出版試閱PDF）自然重疊的**單字本身**不受著作權保護，屬正常現象，但不採用其例句/解析/選字順序 |
| `ielts-vocab` | 雅思單字 | 同多益單字，無官方字表，Claude 自行判斷常見學術/情境用字 |

- 題型（`type`）沿用現有 `"mc"`，UI 直接沿用 `track.js` 現有的 `renderMcQuestion()`，題目格式為「單字＋情境例句挖空／或提供英文釋義」四選一，具體出題格式在實作時比照現有文法題的 JSON 結構（`question`/`options`/`answerIndex`/`explanation`/`optionNotes`）。

---

## 3. 首頁改版

首頁 (`index.html`/`app.js`) 從目前扁平列表，改成依分類分組顯示：

```
文法
├─ 國中文法
├─ 高中/學測文法
├─ 多益 Part 5
├─ 多益 Part 6（即將推出，卡片可點但顯示提示文字，不連結）
└─ 多益 Part 7（即將推出，同上）

單字
├─ 國中單字
├─ 高中單字
├─ 多益單字
└─ 雅思單字

中翻英
├─ 國中中翻英
└─ 高中/學測中翻英
```

標題文字從「每日文法與中翻英練習」調整為含括單字的講法（例如「每日文法、單字與中翻英練習」）。儀表板 (`dashboard.js`) 的兩欄（文法／中翻英）比照改成三欄（文法／單字／中翻英），並新增「今天要複習幾題」統計（見第5節）。

已於對話中做示意圖確認版面（首頁分組＋單字新分類），儀表板三欄示意圖尚未另外產出，實作時再視覺確認一次即可，不需重新走一輪規格討論。

---

## 4. 知識庫回饋同步（單向：測驗網站 → Obsidian）

**技術前提**：`bw-grammar-daily` 是純瀏覽器端網頁，無法自動讀寫本機 `D:\` 磁碟。此功能必須透過**本機腳本**（Claude Code 手動執行的 Python 腳本，非網站本身的功能）完成，讀取 Firestore 資料、寫入本機 `english-study\00-自學\知識庫\*.md`。

### 4.1 內容轉換規則

- **單字題**：一題對應一張卡，套用既有 `Templates\Obsidian-Templates\單字卡範本.md` 格式（詞性/意思/例句/同義詞反義詞/相關/來源），例句取用題目本身，來源欄位標記「bw-grammar-daily {題庫名稱}」
- **文法題**：**一個文法分類（`category` 欄位）對應一張卡**（例如 `假設語氣.md`），不是一題一卡；卡片內容整合同分類底下所有題目的解析，之後同分類新增題目時更新同一張卡（不是新建）
- 新卡若與既有知識庫卡片有具體關聯（比對現有 29 張卡的字/概念），依 `AGENTS.md`「筆記連結規則」加上雙向 `[[wikilink]]`；連結前須先確認目標卡片存在且關聯有意義，不濫連

### 4.2 觸發時機（手動觸發，非即時自動）

符合以下任一條件時，由使用者告知或 Claude 主動提醒，執行一次同步腳本：

1. 題庫有新增/擴充題目時（自然檢查點，新解析內容在此刻出現）
2. 使用者每次完成一輪複習後，會提醒是否要同步
3. 安全網：距離上次同步**超過 7 天**（一週），主動提醒一次

### 4.3 技術需求

- 需要 Firebase Admin SDK 的 service account 金鑰才能在本機腳本讀取 Firestore（目前網站前端用的是公開讀寫規則，但本機腳本走 Admin SDK 更安全、不受瀏覽器限制）
- 金鑰依全域規則存 `.env`，不得寫入程式碼或印出

---

## 5. 間隔重複複習排程（取代現有 needsReview 二元邏輯）

參考 SM-2 演算法標準值（Wozniak, 1987/1990，Ebbinghaus 遺忘曲線為理論基礎）[L2]，因應本站只有「對/錯」二元作答結果（無 Anki 式5級自評），採簡化版：

### 5.1 資料結構異動

`self_grammar/{trackId}/items/{itemId}` 新增欄位：

| 欄位 | 型態 | 說明 |
|---|---|---|
| `interval` | number | 目前複習間隔（天） |
| `easeFactor` | number | 難易度係數，初始 2.5，下限 1.3 |
| `dueDate` | timestamp | 下次應複習的日期 |

### 5.2 排程規則

| 情況 | interval 變化 | easeFactor 變化 |
|---|---|---|
| 第1次答對 | 設為 1 天 | 維持 2.5（新題預設值） |
| 第2次連續答對 | 設為 6 天 | 不變 |
| 之後每次答對 | `interval = interval × easeFactor` | 不變（簡化版，因無5級評分無法用官方公式微調） |
| 答錯 | 重置為 1 天 | `easeFactor = max(1.3, easeFactor - 0.2)` |

`dueDate = 作答當下時間 + interval 天`。範例遞增：1天→6天→15天→37天→92天...（EF=2.5時）。

### 5.3 舊資料遷移

現有題目（已作答過、無 `interval`/`easeFactor`/`dueDate` 欄位者）：首次讀取時若欄位不存在，視為**今天到期**（`dueDate` = 今天），`easeFactor` 初始化為 2.5，`interval` 初始化為 1，讓舊資料自然併入新排程，不需要額外的一次性遷移腳本。

### 5.4 畫面異動

- `wrong.html` 改版為「複習佇列」：顯示規則從「`needsReview === true`」改成「`dueDate <= 今天`」（不分當初對錯），依到期日由舊到新排序
- 首頁／儀表板新增「今天要複習幾題」統計數字（跨全部題庫，含單字＋文法＋中翻英）

---

## 6. 假設與已知限制

- 多益/雅思單字沒有官方字表，選字範圍依賴 Claude 的判斷，難免有主觀性，非官方認證涵蓋率
- 間隔重複的 easeFactor 因為只有二元對錯，調整幅度是簡化估計值，不是嚴格照搬 SM-2 官方5級評分公式算出來的精確值
- 知識庫同步腳本需要額外設定 Firebase Admin SDK 金鑰，是這次新增的技術依賴
- Part 6/7 只有首頁佔位，實際功能未定案時程，之後需要另一輪規格討論（介面設計、內容來源）

---

## 7. 建議實作順序（第一輪，已全數完成並上線）

1. 首頁/儀表板改版（分組顯示、多益改名、Part6/7佔位卡）——純前端改動，風險最低，可先驗證版面
2. 間隔重複排程（資料結構＋ `wrong.html` 改版＋儀表板複習數字）——核心邏輯改動，需要仔細測試新舊資料相容
3. 新增4個單字題庫內容（各10題起始批次）——內容產出，可與前兩項並行
4. 知識庫回饋同步腳本——依賴前面題庫內容已存在，最後做

第一輪完成後，另外透過「Gemini出題、Claude Code審查」協作模式（見 [[english-study/00-自學/bw-grammar-daily/question-writing-template|question-writing-template.md]]），把9個題庫（國中/高中/多益文法、國中/高中/多益/雅思單字、國中/高中中翻英）全數從10題擴充到25題。

---

## 8. 儀表板整併與細節展開（第二輪，已完成並上線）

**決策原則**（使用者明確指示）：判斷標準是「什麼是好系統」，不是「改動大小」，不要因為改動範圍大就傾向保守做法，避免多次小修最終還是走向大改動。

### 8.1 首頁整併進儀表板
- 刪除 `index.html`、`app.js`（功能完全被儀表板取代，不再需要）
- `dashboard.html` 成為唯一入口（8/6已把晨間排程開啟的網址改成直接開 `dashboard.html`，與此決策一致）
- `track.html`、`wrong.html` 目前各自的「回首頁」＋「儀表板」兩顆導覽按鈕，合併成一顆「儀表板」，指到 `dashboard.html`，避免留下死連結

### 8.2 資料結構異動
- `self_grammar/{trackId}/days/{日期}` 新增 `itemIds` 陣列欄位：每次作答時（`track.js` 的 `recordAnswer`）用 `FieldValue.arrayUnion(questionId)` 把該題id加進當天對應的 `days` 文件，讓「這天實際練了哪幾題」可查（現有只存次數，查不出是哪幾題）
- `computeStreak(dayCounts)` 改成同時回傳連續天數的**起訖日期**，不只回傳數字（例如回傳 `{count, startDate, endDate}`），呼叫端跟著調整

### 8.3 儀表板卡片改版：拆成兩個獨立可點擊區
（符合 `WEB_DESIGN_SPEC.md`「可點擊元件視覺區隔」規則：功能不同的可點擊元件需要足夠視覺對比，不可只靠顏色深淺區分——兩區之間要有明顯分隔，例如分隔線／不同底色）

- **上半部**（標題／進度條／已作答／正確率／待複習）：整塊包成 `<a href="track.html?t=...">`，點擊進練習頁（跟首頁卡片行為一致）
- **下半部**（迷你熱力圖／統計數字）：不是連結，點擊在卡片內**展開細節面板**（沿用現有 `.explanation-box`／`.reference-box` 的 `.show` class 切換手法，不做彈窗——手機Safari上彈窗容易有版面跑掉、關閉手勢不直覺的問題，之前已經因為手機捲動出過一次bug，不要在同一個風險點上再加一個）
  - 點熱力圖裡**某一天的小方塊**：列出當天實際作答的題目（用新增的 `itemIds` 欄位查對應題目的 `question` 文字）
  - 點「練習天數」／「連續天數」數字：顯示對應的日期清單／起訖區間
  - **每題各自的作答次數＋正確率清單**，依正確率由低到高排序（最容易答錯的題排最前面，最直接可以拿去複習）
  - 依累積答錯率標示**難度標籤**（例如答錯率 > 50% 標「較難」），借鏡 staresto 網站設計，沿用同一份「每題正確率」資料，不需要額外欄位
  - 每題旁邊加一個**「回報這題有問題」按鈕**，借鏡 staresto 網站設計，點擊記錄題目id（實作時再定要寫進 Firestore 新collection還是先簡化處理）

### 8.4 不在本輪範圍
- **正確率的時間趨勢分析**（例如「這題最近進步了沒」）：現有資料庫只存累計次數，沒存逐次作答的時間序列，做不出來，除非另外改資料結構記錄每次作答紀錄，這是更大的改動，本輪不做
- **多益Part6/7實際功能**：見第9節，只記錄內容來源原則，介面/資料結構/頁面路由仍待另一輪規格討論
- **依難度/年份篩選練習範圍**：staresto另一項借鏡設計，題庫量還不夠大，此功能有意義的時機還沒到，暫不做

---

## 9. 多益 Part 6/7 內容來源原則（先定案，實作仍不在本輪範圍）

本節只記錄**內容來源的判斷原則**，供之後真正要規劃 Part 6/7 介面與實作時直接沿用，不需要重新討論這部分。

### 9.1 閱讀測驗類（新聞/文章主題）
- 可以參考 Taipei Times Bilingual Pages、CNN、BBC Languages 等真實媒體，**但僅作為「這種主題/難度/字彙程度是常見的」方向參考**
- 查證發現這些媒體頁面明確標示「All rights reserved」，沒有開放轉載聲明，屬於真正的著作權風險，跟通用文法句型的灰色地帶性質不同 [L4，未查證個別授權條款細節]
- **不照抄任何一篇具體文章的情節/細節去改寫**（即使換句話說，貼著同一篇真實文章的具體敘事走，仍可能構成衍生作品）；改為在確認主題類型後，自己發明全新的虛構情境（虛構公司、人物、數據、地點）
- 每篇虛構閱讀文章加註「（改寫，不保證事實）」，避免使用者誤以為是真實地點/事件的報導——這個標示解決的是「誤導疑慮」，不是著作權疑慮，兩者分開處理，不能只靠標示就當作著作權問題已解決

### 9.2 商業書信類（email/memo/letter）
- 格式本身（稱謂/主旨/正文/結尾敬語）是通用範本，不受著作權保護，可直接使用
- 格式/題型結構可參考 ETS 官方公開的 examinee handbook 範例（ETS 本身就是多益出題單位，公開範例的目的就是給考生參考，比引用新聞媒體更沒有疑慮）
- 具體情境內容（虛構公司/人物/情境，例如請假信、客訴回覆、會議通知、產品發表通知）由 Claude 自行發明，比照現有多益單字/文法題庫的做法

---

## 10. 建議實作順序（第二輪）

1. 資料結構異動（`days.itemIds`、`computeStreak` 回傳日期範圍）——先做這個，之後畫面才有資料可用
2. 首頁刪除、`track.html`/`wrong.html` 導覽按鈕合併——需要確認沒有殘留死連結
3. 儀表板卡片拆兩區塊＋展開細節面板（含難度標籤、錯誤回報按鈕）——這輪UI改動最大的部分，本機伺服器多次視覺驗證後再上線
4. 第9節（多益Part6/7）留待下一輪規格討論，不在這次實作範圍內

---

## 11. 待辦（下一輪：視覺美化）

功能面（第8節）確認可用後，使用者回饋視覺美觀不足，明確要求**先排入待辦，等目前這輪功能性修改全部結束後再一起討論**，本輪不動手：

- 字體大小需要調整（哪些地方偏小/偏大未細談，待討論時具體確認）
- 增加色塊區分（目前卡片內兩個可點擊區只用一條虛線分隔，可能不夠明顯）
- 加入 icon 圖案（哪些地方加、用什麼圖示未細談）

下次要開始這輪視覺美化前，需要先重新問一次具體想要調整哪些地方，不要自己憑空猜。

---

## 12. 多益 Part 6：完整架構（第三輪，本次要動手的範圍）

**範圍**：這次只做 Part 6。Part 7 維持第9節的內容來源原則記錄，實際實作繼續留待之後。

### 12.1 官方格式驗證

查證 toeic.com.tw（春晉有限公司，多益官方台灣代理機構）釋出的官方範例 [L2]，確認 Part 6 每篇文章4格空格分四種類型，且解題策略各不相同：

| 空格類型 | 判斷特徵 | 對應現有題庫分類 |
|---|---|---|
| 詞性題 | 選項是同一個字的不同詞性衍生（decide/decision/decisive） | 沿用既有「詞性判斷」分類 |
| 時態題 | 選項是同一動詞不同時態 | 沿用既有「時態」分類 |
| 單字/片語題 | 選項是意思不同的字，需看前後1-2句判斷語意 | 沿用單字題庫詞性分類（動詞/名詞/形容詞）或既有介系詞/連接詞分類 |
| 句子插入題 | 選項是四個完整句子，Part 6失分重災區 | 獨立分類「段落邏輯」，其他題庫沒有對應 |

句子插入題的解題「三大定位關鍵」（Gemini協作提供，經比對官方範例與多方教學來源一致）：**指示代名詞**（this/these/such/it，代表前句提及該事物）、**邏輯轉折詞**（however/therefore/in addition等）、**詞彙關聯性**（選項核心名詞與前後句主題詞彙相關）。

### 12.2 資料結構

新增 track `toeic-part6`，JSON schema跟現有扁平題庫不同，改成「一篇文章＋一組小題」：

```json
{
  "id": "p6-01",
  "passageType": "email",
  "passage": "文章全文，4個空格處標記",
  "chinese": "整篇中文翻譯（可選）",
  "questions": [
    { "blankType": "詞性判斷", "options": [...], "answerIndex": 0, "explanation": "...", "optionNotes": [...] },
    { "blankType": "時態", ... },
    { "blankType": "動詞", ... },
    { "blankType": "段落邏輯", "options": ["四個完整句子"], "answerIndex": 0, "explanation": "須明確指出三大定位關鍵中的哪一個" }
  ]
}
```

**Firestore 資料顆粒度是「整篇文章」，不是單一小題**：`self_grammar/toeic-part6/items/{passageId}` 的 `attempts`/`correctCount` 都是這篇文章層級的次數，不是四小題各自累計。`days` 子集合沿用現有 `count`+`itemIds` 模式。

**已知範圍限制**：Part 6 四小題各自的 `blankType` 分類**不會併入**現有「文法錯誤類型統計」（跨題庫合計），因為那個統計的資料顆粒度是「一個Firestore項目=一題」，Part 6 是「一個項目=一整篇文章」，兩種顆粒度對不上，這次不強行湊在一起。`blankType` 標籤只用在解析文字撰寫規則上（見12.4）。

### 12.3 複習排程：以整篇文章為單位

四小題**全部答對**才算這次「答對」，套用現有的 SM-2 排程（`nextSchedule`）；只要有一小題答錯，整篇文章都判定為這次「答錯」，`interval`重置1天、`easeFactor`調降，之後複習佇列會整篇文章一起再排入，不是只排錯的那一小題（維持文章上下文完整）。

### 12.4 內容撰寫規則

- **句子插入題解析**：必須明確指出這題的線索是「指示代名詞／邏輯轉折詞／詞彙關聯性」三者中的哪一個，具體點出是哪一句提供的線索，不能寫「通讀全文才懂」這種空泛說法（不符合實際解題技巧：這類題目只需看空格前後1-2句）
- **文章開頭依體裁慣例**：email/letter開頭直接說明目的；memo要交代收件對象/主旨/施行日期；notice/article要交代Event/When/Action Required
- **內容來源**：沿用第9節原則（自創虛構情境，不照抄任何具體來源的文章內容）；toeic.com.tw官方代理機構的範例可作為格式驗證的參考來源，比第三方教學網站/商業書籍證據力更高，但一樣不照抄具體例句

### 12.5 頁面設計（已完成）

新增 `part6.html` + `part6.js`（沒有沿用 `toeic-part6.html` 這個命名，改成跟 `track.html`/`track.js` 一樣的通用頁面模式，用 `?t=` 參數指定 track，理由：頁面邏輯本來就是靠 track.type 分派，不是綁死單一 track，之後如果 Part 6 以外還有同結構的文章題型也能共用）：畫面上方固定顯示整篇文章全文（含已標記的4個空格位置，正在作答的那格醒目標示、答過的格子依對錯上色），下方依序呈現4小題讓使用者作答，沿用現有 `track.js` 的選項按鈕/解析框UI元件與樣式（不重新設計視覺），4題都答完後才依12.3規則寫入 Firestore 判定整篇對錯並換下一篇文章。`nextSchedule()` 排程函式從 `track.js` 搬到 `tracks.js` 共用。

### 12.6 首頁/儀表板呈現（已完成）

- `tracks.js` 的 `COMING_SOON` 移除「多益 Part 6」項目，新增為 `TRACKS` 正式項目（`id: "toeic-part6"`, `type: "part6"`, `group: "grammar"`，跟其他文法題庫並列）
- 儀表板/首頁卡片行為（點擊進入、細節展開等）直接沿用第8節已完成的機制；卡片連結目標依 `track.type === "part6"` 判斷導向 `part6.html` 還是 `track.html`（`dashboard.js`/`wrong.js` 各補一處判斷）

---

## 13. 建議實作順序（第三輪，已全部完成）

1. ~~撰寫 `toeic-part6.json` 起始內容~~ 完成：5篇文章＝20題（Gemini撰稿、Claude Code審查後合併，answerIndex 20題剛好5/5/5/5平均分布，未發現需要重排的問題）
2. ~~`track.js` 或新增 `part6.js`：處理整篇文章顯示＋4小題依序作答＋12.3的整篇文章判定邏輯~~ 完成：新增 `part6.js`
3. ~~`tracks.js` 更新（COMING_SOON移除、TRACKS新增）、首頁/儀表板自動套用現有渲染邏輯~~ 完成
4. ~~本機伺服器驗證~~ 完成：文章顯示、4小題依序作答、整篇文章判定邏輯（含review模式單篇複習）、儀表板分母/正確率/待複習旗標、複習佇列連結目標，皆已在本機測過並清除測試資料
5. 第9節其餘部分（Part 7）留待之後另一輪規格討論

---

## 14. 單字/片語內容豐富化＋答題揭曉答案強化（第四輪）

### 14.1 單字選字依據校正

- `junior-vocab`／`senior-vocab` 原本就已經規定要依官方字表選字（見第2節），但實際委託 Gemini 出題時沒有明確要求「依序、分級挑字」，容易漏字或選字偏好集中。這次起，委託提示語明確要求依字表順序或級別挑字，確保覆蓋率。
- `toeic-vocab` 沒有真正的官方字表（TOEIC是ETS私人測驗，不是政府公告），維持第2節原本「Claude/Gemini自行判斷職場常用字」的自訂方式，不硬套「官方字表」的說法。
- `ielts-vocab` 維持自訂學術字彙，不變。
- **同一個單字可以跨 `junior-vocab`／`senior-vocab` 重複出現**，不用強制去重——國高中對同一個字考的深度本來就該不同（國中考基礎字義，高中考進階用法/同義字辨析/搭配詞），這是分級設計的用意。「已有內容清單」只需避免同一題庫內部重複，不用跨題庫比對。

### 14.2 新增 `etymology` 欄位（選填）

單字題庫（4個vocab track）JSON schema 新增選填欄位 `etymology`（字串），依題庫性質填不同內容：

- `junior-vocab`／`senior-vocab`／`toeic-vocab`：字根字首拆解，例如 `"bene-(好) + -volent(意願) → 心懷善意的"`
- `ielts-vocab`：字根字首拆解，或語源親緣連結（格林法則p→f/t→th/k→h等音變規律，連結日耳曼語源常用字跟拉丁/希臘語源學術字，例如 `"foot(日耳曼) 與 pedal/pedestrian(拉丁ped-)同源，格林法則 p→f"`），依單字性質擇一或都不填
- 不是每個字都硬要湊字根（例如cat這種基礎字沒有好拆的字根，留空即可），沒有的話就不寫這個欄位
- 目前只當作解析內容的延伸資訊顯示，**不做儀表板統計**（題量還小，跟第12.2節Part 6的`blankType`不併入統計是一樣的道理，之後題量夠大再考慮）

### 14.3 片語新增為既有題庫的分類，不開新題庫

- 片語內容不開新 track，用既有題庫的 `category` 欄位加一個新值 `"片語"` 即可：
  - 多益情境固定片語（in charge of／be eligible for等）→ 加進 `toeic-grammar`（多益 Part 5，本來就是文法+字彙混合）
  - 國高中課本片語（look forward to／give up等）→ 加進對應的 `junior-vocab`／`senior-vocab`
- JSON schema不變（`category`／`question`／`options`／`answerIndex`／`explanation`／`optionNotes`都已支援），純粹是內容分類多一個標籤

### 14.4 答題揭曉答案：題目文字直接顯示完整正確句子並標色

範圍：**所有 mc 類型題目**（文法+單字，共用 `track.js` 的 `renderMcQuestion()`），不限片語題，理由是同一套機制、多判斷題目類型沒有實質好處。

- 觸發方式：沿用現有「點選選項按鈕」流程（mc類型本來就是點選作答，不是輸入），不新增輸入介面
- 點選任一選項（不管答對答錯）後，題目文字裡的「______」直接替換成正確答案文字，並用醒目顏色標示（例如 `<mark>` 或自訂 class），跟解析框一起呈現，不另外加一行「完整句子：...」重複顯示
- 效果：使用者看到的是「完整正確句子＋答案處變色」，強化記憶，不用自己在腦中拼湊空格前後文意

### 14.5 解析內容撰寫規則新增

不新增資料欄位，直接補進 `explanation` 文字內容的撰寫規則（[[english-study/00-自學/bw-grammar-daily/question-writing-template|question-writing-template.md]] 同步更新）：

- **延伸同義字/反義字/常用搭配詞**：解析結尾視情況補一句相關延伸，例如測 borrow 時帶一句「反義：lend（借出）」，不用每題都硬湊，字詞本身沒有明顯對應時可以不寫
- **交叉知識點提示**：如果這題的解析剛好用到另一個知識點（例如單字題解析提到「過去分詞當形容詞」這個文法概念），明確點出來，例如在解析裡加一句「（順便複習：分詞構句）」，讓使用者意識到自己不只學到這題答案，還複習到別的知識點——這個現象本來就常常自然發生（Gemini撰寫解析時會提到），這次把它變成刻意的撰寫規則，不是碰運氣

---

## 15. 建議實作順序（第四輪）

1. 前端功能（14.4）：修改 `track.js` 的 `renderMcQuestion()`，點選任一選項後把題目文字的「______」換成正確答案並加上標色樣式，`style.css` 新增對應 class
2. 內容規則更新：[[english-study/00-自學/bw-grammar-daily/question-writing-template|question-writing-template.md]] 補上14.1（依字表順序選字、跨題庫可重複用字）、14.2（`etymology`欄位規則）、14.3（片語分類規則）、14.5（延伸同義字/交叉知識點撰寫規則）
3. 本機伺服器驗證14.4的畫面效果（答對/答錯兩種情況、確認解析框跟標色文字沒有版面衝突）
4. 準備下一批 Gemini 委託提示語（junior-vocab／senior-vocab 依官方字表續編、片語內容、`etymology`欄位、延伸解析規則），數量待與使用者確認後再委託

---

## 16. 複習佇列自動接續＋剩餘題數計數器（第五輪）

### 16.1 問題

現況：從複習佇列（`wrong.html`）點「重新練這一題」進入 `track.html`／`part6.html` 的單題複習模式（`?q=`參數），答完該題後顯示「複習完成！」畫面，需手動點「回複習佇列」才能繼續下一項，多題待複習時要反覆手動點擊、每題都要繞一次列表頁。

### 16.2 資料流程

- `loadDueItems()` 從 `wrong.js` 搬到 `tracks.js` 共用（邏輯不變：查全部題庫的 `items`，篩到期項目，依到期日排序，回傳含 `track`／`questionId`／`dueDate` 的清單）
- `track.js`／`part6.js` 偵測到 `?q=` 參數（複習模式）時，頁面載入當下呼叫一次 `loadDueItems()`，取得當下完整待複習清單（含自己這一題），存在頁面內的變數：
  - 用途一：顯示「剩餘N題」計數器（16.3）
  - 用途二：答完這題後，從這份清單移除目前這題，若還有剩下的項目，直接導向清單裡到期日最早的下一項；清單已空才顯示「全部複習完成」畫面
- **不使用 sessionStorage 等額外前端狀態儲存**——每次進頁面都直接查 Firestore 當下最新到期清單，避免前端暫存跟資料庫實際狀態不同步；清單只在單次頁面停留期間使用，換頁後由下一頁重新查詢

### 16.3 剩餘題數計數器樣式

- 只在複習模式（`?q=`存在）時顯示；一般選題庫整個練習模式不顯示
- 樣式：黃底 `#ffe135`、黑框 `3px solid #1c2230`、硬邊偏移陰影 `4px 4px 0 #1c2230`（不用模糊/發光效果）、圓角矩形或藥丸形，文字「剩餘 N 題」，數字用 `font-variant-numeric: tabular-nums` 避免題數變動時寬度跳動
- 位置：`track.html`／`part6.html` 的 header 區塊內新增一個插槽（例如 `#reviewCounterSlot`），複習模式時由JS填入，非複習模式維持空白

### 16.4 全部複習完成畫面

- 取代原本「複習完成」（`isReviewMode`分支）畫面，卡片樣式不變，文字改為「今天的複習都做完了！」
- 按鈕改為「回複習佇列」＋「回儀表板」，移除原本的「做整個題庫」——因為情境是橫跨多個題庫的到期項目一次複習完，不對應單一題庫

### 16.5 跨頁面過場

- Part6待複習項目在 `part6.html`、其他題庫在 `track.html`，是兩個獨立頁面；下一項如果剛好是不同頁面類型，用 `location.href` 整頁導向（不是同頁無感切換），但比現在「答完要先回列表再點下一個」快很多

### 16.6 `wrong.html` 本身不變

- 複習佇列列表頁維持現狀（列出所有到期項目，各自可點進去），不強制從第一項開始——不管從哪一項進入複習模式，答完都會自動接續清單裡其餘到期項目，直到清單空了才顯示完成畫面

---

## 17. 建議實作順序（第五輪）

1. `loadDueItems()` 從 `wrong.js` 搬到 `tracks.js` 共用，`wrong.js` 改呼叫共用版本
2. `style.css` 新增 `.review-counter` 相關樣式（16.3）
3. `track.html`／`part6.html` header 新增 `#reviewCounterSlot` 插槽
4. `track.js`／`part6.js`：複習模式載入時查詢待複習清單、顯示計數器；`renderDone()` 的 `isReviewMode` 分支改為「清單非空就導向下一項，清單空才顯示全部複習完成畫面」（16.2、16.4）
5. 本機伺服器驗證：多題待複習時的自動接續（含跨track.html/part6.html換頁）、計數器數字正確遞減、全部複習完的最終畫面、清單本來就只有1題時的行為（應與現行單題複習體驗一致）

---

---

## 18. 句子結構／分類辨識題型（第六輪）

### 18.1 背景與設計理念

使用者提出的學習觀察：寫文法題時，腦中會先判斷「這是什麼類型」再選答案。這符合TOEIC官方Part 5解題策略（先判斷題目考詞性/時態/介系詞/子句，再看選項），也符合認知科學對專家處理複雜問題的共通觀察——先辨識模式、再套用對應知識，新手才是逐一嘗試每個可能性 [L4，一般教學法/認知科學常識，未查證特定研究]。現有填空題只**隱性**訓練這個判斷能力（使用者默默在腦中做，題目本身沒有直接考），這次新增的題型把它獨立拉出來**明著考**，讓判斷類型跟套用文法規則這兩個能力可以分開練、分開強化。

### 18.2 三種新出題角度

共通機制：沿用現有mc格式schema（`question`/`category`/`options`/`answerIndex`/`explanation`/`optionNotes`可選），**不需要新增欄位或新頁面**——`options`本來就可以放分類標籤或句子片段，不是只能放單字，這是這次的核心發現。

1. **句子結構（子句判斷）**：給一個完整句子，選項是句子裡的不同片段，題目問「哪個部分是主要子句／名詞子句／形容詞子句／副詞子句」。同一句話如果要考多個子句類型，**拆成多題各自問**，不做成一次配對多答案的介面（避免過度工程，現有4選1格式已足夠達到教學效果）。

   範例：
   ```json
   {
     "question": "The manager who arrived late apologized to the client for the delay.",
     "category": "句子結構",
     "options": ["The manager", "who arrived late", "apologized to the client for the delay", "for the delay"],
     "answerIndex": 2,
     "explanation": "..."
   }
   ```

   **深化變化（Gemini提供，2026-08-07審查採納）**：不只問「哪個片段是名詞子句」，也可以先標出子句、問它在句中的**功能**（主詞/受詞/主詞補語/同位語），訓練拆解長難句、抓句子主幹的能力，對多益Part 7閱讀有幫助。例如：「在這句話中，名詞子句 that the project was delayed 扮演什麼角色？」選項：主詞／受詞／主詞補語／同位語。

2. **時態判斷**：給一個完整句子，選項可以是時態名稱標籤（現在完成式/過去簡單式等），或句子片段（哪個部分顯示這是完成式），題目問「這句話用的是什麼時態」或「哪個部分顯示時態」。

   **深化變化（Gemini提供，2026-08-07審查採納）**：不只問「這是什麼時態」，也可以反過來問「哪個片段（時間線索詞）決定了這裡必須用這個時態」，直接對應多益Part 5的核心解題技巧（看到by the time／since／for the past等線索詞就知道該用哪個時態），比單純認時態名稱更貼近實際應試能力。

3. **假設語氣類型判斷**：給一個完整句子，選項是分類標籤（與現在事實相反/與過去事實相反/真實條件句/與未來事實相反），題目問「這句話是哪一種假設語氣」。

   範例：
   ```json
   {
     "question": "If I had studied harder, I would have passed the exam.",
     "category": "假設語氣",
     "options": ["與現在事實相反", "與過去事實相反", "真實條件句", "與未來事實相反"],
     "answerIndex": 1,
     "explanation": "..."
   }
   ```

   **深化變化（Gemini提供，2026-08-07審查採納）**：不只問「這是哪種假設語氣」，也可以反問「這句話暗示的實際客觀事實是什麼」（例如上例反推：預算當時沒被批准），直接打中「背熟公式卻無法轉換成真實語意」的常見盲點，確保理解而非死背句型。

### 18.3 分類歸屬原則

- **只有「句子結構（子句判斷）」是全新知識點**，獨立開 `"句子結構"` 分類
- **「時態」「假設語氣」判斷題沿用既有分類名稱，不另開新分類**——雖然出題角度不同（辨識而非填空），但考的知識點跟既有分類相同，另開新分類會讓文法錯誤類型統計把同一個知識點拆成兩條、失真（同一原則見第2節「已有內容清單」跨題庫比對規則）

### 18.4 適用題庫

- 句子結構（子句判斷）：子句嵌套對國中生偏難，建議只放 `senior-grammar`／`toeic-grammar`
- 時態、假設語氣判斷題：`junior-grammar`／`senior-grammar`／`toeic-grammar` 三個文法題庫都可以用，依程度調整句子複雜度

### 18.5 已知限制

這類「辨識題」答案選項本身就是完整標籤或句子片段，不是單一空格的填空，第14.4節的答案標色功能（`fillAnswerIntoQuestion()`）不適用也不需要修改——題目句子本身不含 `______` 空格標記，維持原樣顯示即可。

---

## 19. 建議實作順序（第六輪）

1. 更新 [[english-study/00-自學/bw-grammar-daily/question-writing-template|question-writing-template.md]]：新增這3種出題角度的格式說明、範例、分類歸屬原則
2. 委託Gemini出題（題數與優先題庫待與使用者確認）
3. Claude Code審查合併
4. 本機驗證顯示效果，**特別測手機小螢幕視窗**（Gemini審查提醒，2026-08-07）：這3種題型的`options`常是完整句子片段而非單字，字數較長，須確認`track.html`的選項按鈕（`.option-btn`）在窄螢幕下能正常換行，不會被截斷或跑版

---

*本文件建立：2026-08-04；第二輪擴充章節（第8-10節）新增：2026-08-06；Part 6 實作完成：2026-08-05；第四輪（單字/片語豐富化＋答案標色）新增：2026-08-05；第五輪（複習佇列自動接續）新增：2026-08-07；第六輪（句子結構/分類辨識題型）新增：2026-08-07*
